export enum PageEnum {
  // basic login
  BASE_LOGIN = "/login",
  // basic home
  BASE_HOME = "/dashboard",
  // error page
  ERROR_PAGE = "/exception",
  // error log page
  ERROR_LOG_PAGE = "/error-log/list",
}
